#include<stdio.h>
#include<conio.h>
void main()
{
int ch;
printf("Enter the letter is to be check vowel or not?");
scanf("%d",&ch);
if(ch==a && ch==i && ch==o && ch==e && ch==u)
{
printf("The letter is vowel");
}
else
{
printf("The letter is consonant");
}
getch();
}
